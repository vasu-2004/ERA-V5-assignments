# Session 2 — Instructor's Reference Solution (dump here)

Drop everything from the instructor's download link into this folder:

```
tokenizer.json
metrics.json
build_wiki_faithful_markdown.py
train_tokenizer.py
evaluate_tokenizer.py
corpus/*.faithful.md
corpus/*.faithful.txt
corpus/*.meta.json
```

Once the files are here, the assistant will read the exact recipe + metric and the
faithful-Markdown corpus, then reuse them to build the resubmission in
`../session-2-assignments-resubmitted/`.

_Placeholder file so the (otherwise empty) folder is visible in git/GitHub._
